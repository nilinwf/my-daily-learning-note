# 代码随想录算法训练营74期|第三章 哈希表part02 


| 题目     | Leetcode地址 |
| ----------- | ----------- |
|151.反转字符串里的单词 | [力扣题目链接](https://leetcode.cn/problems/reverse-words-in-a-string/)      |
|卡码网：55.右旋转字符串 | [题目链接](https://kamacoder.com/problempage.php?pid=1065)      |
|28.找出字符串中第一个匹配的下标 | [力扣题目链接](https://leetcode.cn/problems/find-the-index-of-the-first-occurrence-in-a-string/description/)      |
|459.重复的子字符串 | [力扣题目链接](https://leetcode.cn/problems/ransom-note/)      |



151.反转字符串:使用双指针，注意的是，python可以直接赋值而不需要temp临时存储；

    -1.移除多余空格:这里需要考虑逻辑,fast往下遍历，当s[fast]!= ' '时，如果slow!= 0,此时需要加空格【因为意味着不是第一个单词了】，当fast < n且s[fast]!= ' '，复制字符串，slow++，fast++；否则fast++;得到['t', 'h', 'e', ' ', 's', 'k', 'y', ' ', 'i', 's', ' ', 'b', 'l', 'u', 'e']
    
    -2.合并字符串
    
    -3.将每个单词反转

28.KMP算法：字符串匹配问题，**暴力匹配**O(m*n)或者是**使用前缀表**

前缀：不包含尾字符串；后缀：不包含首字母的字符串

KMP中，如何找到之前匹配过的地方的后面继续匹配，先找到不匹配位置f的前面前缀匹配的后边，比如

文本串：aabaabaaf，模式串：aabaaf此时f位置冲突了不匹配了，那与其相等的前缀为aa，与下表为0的aa匹配，的后边即b的位置开始匹配。例如，此时要求最长相等的前后缀aabaa，此时最长相等前后缀长度为2最大值，刚好就是我们要找的下标为2的，因为下标从0开始，最长前后缀的后边一个就是b的位置！！！！太强了记得返返回来看看，，理解逻辑并加深印象。

代码实现：具体例子讲解由于f的位置冲突了，所以我们看的是f的前一位，a对应的最长相等字符串为2，所以我们的指针就调到了下表为2的位置。**遇见冲突的位置向前回退**

前缀表getNext()函数构造：

    -1.初始化
    
    -2.前后缀不相同 j回退
    
    -3.前后缀相同 j前进
    
    -4.next

i：指向后缀末尾位置，j：指向前缀末尾位置，同时代表i包括i的前面的最长相等字符串

此处使用前缀表作为索引下标，

## KMP算法一定要学通透!!!反复看视频了3遍，getNext()函数理解但是还没太熟

459.重复的子字符串：若非暴力解法，使用移动匹配的核心为：去除字符串的首元素+去除字符串尾字母的字符串，得到的新字符串如果能找到原来的字符串s，则说明是包含重复的子字符串;

判断方法为，得到的next前缀，原字符串最长的长度 % ( 原字符串最长的长度 - 最长相等字符串长度) == 0的话，原字符串即为重构字符串。

