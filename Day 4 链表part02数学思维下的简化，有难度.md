# 代码随想录算法训练营74期|链表part02 交换节点、链表相交、环形链表

| 题目     | Leetcode地址 |
| ----------- | ----------- |
|24.两两交换链表中的节点| [力扣题目链接](https://leetcode.cn/problems/swap-nodes-in-pairs/)      |
|19.删除链表的倒数第N个节点| [力扣题目链接](https://leetcode.cn/problems/remove-nth-node-from-end-of-list/)        |
|160.链表相交             |[力扣题目链接](https://leetcode.cn/problems/intersection-of-two-linked-lists-lcci/)   |
|142.环形链表ii|[力扣题目链接](https://leetcode.cn/problems/linked-list-cycle-ii/)   

# 两两交换链表中的节点
  1.判断条件 curr.next != None and curr.next.next != None
  2.需要提前保存指针 curr到curr.next，但是需要保存dummy_head到节点1的指针temp，同时还需要保存原本节点2到节点3的指针

# 19.删除链表倒数第N个节点
  1.倒数第N个节点，巧妙使用快指针fast与慢指针slow，当fast快于slow个N点之后，快慢指针一起直到快指针指向None，此时慢指针所在位置即为倒数第N个，但针对倒数第N个的前一个指向第N+1个操作，所以只要fast快于slow个N+1即可；

# 160.链表相交 参考【https://leetcode.cn/problems/intersection-of-two-linked-lists/】
  先遍历两个链表，得到两个链表的长度，然后让长的链表先走，直到两个链表的长度相等，然后再一起走，直到两个链表相等。
# 142.环形链表
快指针一次2个节点，慢指针一次1个节点，相对速度是1个节点，所以如果存在环一定会相遇**在判断时需要注意，判断的是节点是否相等，而不是节点的值是否相等**。

- 首先需要解决的是，**如何判断链表是否有环？**：
     以操场跑步为例，操场是一个环，那么快的人永远会追上慢的人（套圈）。所以，可以设置一个快慢指针，快指针每次走两步，慢指针每次走一步，如果快指针追上了慢指针，那么就说明链表有环。
- 其次是，**如何判断链表的环的入口？**：
   这里需要用到一些简单的数学知识：
   利用快指针是慢指针的两倍去构造等式
   慢节点走过的路程为x + y.为什么不是x + n*(y + z) + y呢？因为，如果慢节点能走过一圈，那么快指针应该已经走完了两圈，在这之前，两个指针早就相遇了，所以，慢节点不会走完一圈，所以，慢节点走过的路程为x + y.<br>
   快节点走过的路程为x + n*(y + z) + y 
   由等式关系可知，快指针的路程是慢指针的两倍。
   所以，x + n*(y + z) + y = 2*(x + y)
   化简得：x = (n - 1)*(y + z) + z
   这里的n是一个常数，所以，x = z
   所以，当快慢指针相遇时，将快指针重新指向头节点，然后快慢指针同时走，当快慢指针相遇时，相遇的节点就是环的入口。
  
  【参考研二的】
# 链表总结：【https://www.programmercarl.com/%E9%93%BE%E8%A1%A8%E6%80%BB%E7%BB%93%E7%AF%87.html#%E9%93%BE%E8%A1%A8%E7%9A%84%E7%90%86%E8%AE%BA%E5%9F%BA%E7%A1%80】
- 链表的种类主要为：单链表，双链表，循环链表
- 链表的存储方式：链表的节点在内存中是分散存储的，通过指针连在一起。
- 链表是如何进行增删改查的。
- 数组和链表在不同场景下的性能分析。

